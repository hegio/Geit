# TVBox 自动采集 · 部署教程

照着做，10 分钟内把这个「每 6 小时自动采集 TVBox 配置 + 自动生成订阅页」的机器人跑起来。
全程只需要一个 **GitHub 账号**，不需要自己的服务器。

---

## 一、准备

你需要这些文件（本目录已备齐）：

```
fetch_tvbox.py                  采集脚本（需 pycryptodome 解密）
sources.txt                     采集目标（已默认配好两个地址）
repo.txt                        本地测试用的仓库标识（Actions 会自动覆盖，可不管）
.github/workflows/collect.yml   定时任务（每 6 小时）
README.md                       功能说明
```

云端工作流已自动安装 `pycryptodome`；本地测试先执行 `pip install pycryptodome`。

- 仓库建议设为 **Public**：这样 GitHub Pages 和 jsDelivr 订阅链接才免费可用。
- 一个 GitHub 账号（免费）。

---

## 二、在 GitHub 新建仓库

1. 打开 https://github.com/new
2. **Repository name**：随便起，例如 `tvbox-mirror`
3. **Visibility**：选 **Public**
4. 不要勾选 "Add a README" / .gitignore / License（保留空仓库，避免冲突）
5. 点 **Create repository**

---

## 三、把文件上传到仓库

### 方式 A：网页直接上传（最简单，推荐新手）

1. 进入刚建好的仓库页面
2. 把本目录里的以下文件/文件夹全部拖进上传区（或点 "uploading an existing file"）：
   - `fetch_tvbox.py`
   - `sources.txt`
   - `repo.txt`
   - `.github/` 整个文件夹（里面是 `workflows/collect.yml`）
   - （可选）`README.md`
3. 在底部 **Commit changes** 写个说明，如 `init: tvbox auto collector`，确认提交

> 注意：必须把 `.github` 文件夹一起传上去，否则定时任务不会生效。

### 方式 B：用 Git 命令（熟悉命令行的同学）

```bash
# 1. 克隆你的空仓库（换成你自己的地址）
git clone https://github.com/你的用户名/tvbox-mirror.git
cd tvbox-mirror

# 2. 把本目录里的文件复制进来（含隐藏的 .github）
cp -r /path/to/tvbox-collector/* .
cp -r /path/to/tvbox-collector/.github .

# 3. 提交并推送
git add -A
git commit -m "init: tvbox auto collector"
git push
```

推送时若要求账号密码，密码处填 **Personal Access Token**（GitHub 已不支持账号密码推送），
在 GitHub → Settings → Developer settings → Personal access tokens 生成一个带 `repo` 权限的 token。

---

## 四、确认配置（一般不用改）

| 文件 | 要不要动 | 说明 |
|---|---|---|
| `sources.txt` | 通常不用 | 已默认配好：lytvs.top 单文件 + `REPO:FGBLH/HKL` 整仓库镜像（抓全部 .py/.js/.json 等核心文件，含自动解密 `ok海豚18`） |
| `repo.txt` | 不用 | Actions 运行时会自动用环境变量 `GITHUB_REPOSITORY` 覆盖 |

脚本支持自动解密「肥猫工具箱」AES-128-CBC 加密 TVBox 接口。遇到无扩展名、内容全是十六进制且以 `$#` 开头的文件时，会自动解密并保存为 `.json`，然后继续抓取其中引用的 `.py` / `.js` / `.json` / `.txt` / `.m3u` 等核心文件。

---

## 五、开启自动运行

GitHub Actions **默认就是开启的**，上传 `.github/workflows/collect.yml` 后就会生效。

1. 进入仓库 → 顶部 **Actions** 标签页
2. 左侧应看到工作流 **TVBox 自动采集**
3. 第一次建议手动跑一次验证：
   - 点进该工作流 → 右侧 **Run workflow** → **Run workflow**
4. 等 1～2 分钟，看到绿色 ✓ 即成功；点进运行记录能看到日志
5. 之后它会**每 6 小时（UTC：00:00 / 06:00 / 12:00 / 18:00）自动跑**

> 时区说明：cron 是 UTC。想改成北京时间每 6 小时，可在 `collect.yml` 里把
> `cron: '0 */6 * * *'` 改成 `cron: '0 0,6,12,18 * * *'`（北京时间 = UTC+8，
> 即 UTC 16:00/22:00/04:00/10:00 对应北京 00/06/12/18，按需换算即可）。

---

## 六、开启订阅页（GitHub Pages）

订阅页让用户/播放器能直接访问你采集到的配置。

1. 仓库 → **Settings → Pages**
2. **Build and deployment → Source** 选 **Deploy from a branch**
3. **Branch** 选 **main**，**目录** 选 **/docs**
4. 点 **Save**
5. 等 1～2 分钟，访问 `https://你的用户名.github.io/tvbox-mirror/` 即可看到订阅页

> 前提：仓库是 **Public**。Private 仓库的 Pages 和 jsDelivr 链接都无法对外访问。

---

## 六·A、Telegram 推送通知（可选）

每次采集结束，机器人会往你的 Telegram 发一条汇总：成功/总数、失败列表、订阅页链接、运行时间。配好之后完全自动。

**第 1 步：找 [@BotFather](https://t.me/BotFather) 建机器人**
1. 给 BotFather 发 `/newbot`
2. 按提示填名字、用户名（用户名必须以 `bot` 结尾，如 `tvbox_collector_bot`）
3. 建好后它会返回一长串 `HTTP API Token`，形如 `123456789:AAE****`，先复制下来

**第 2 步：拿到你的 Chat ID**
1. 给你的机器人发**任意一条消息**（例如打个 `hi`），否则它不知道该发给谁
2. 打开 [@userinfobot](https://t.me/userinfobot)，发 `/start`，它会回你的 `Id`（纯数字；群聊的 ID 是负号开头，如 `-1001234567890`）

**第 3 步：把两个密钥填进 GitHub Secrets**
1. 仓库 → **Settings → Secrets and variables → Actions → Secrets → New repository secret**
2. 新增两个：
   - Name：`TG_BOT_TOKEN`　Value：第 1 步的 Token
   - Name：`TG_CHAT_ID`　　Value：第 2 步的 Chat ID
3. 保存

**第 4 步：生效**
推送 / 重新运行一次工作流即可。没配这两个 Secret 时脚本会**自动跳过推送**（照常采集、不报错）；Token 失效时只记日志、不影响采集结果。

---

## 六·B、tvbox/py.json（蜘蛛 + 直播源配置）

脚本每次运行除了订阅页，还会在 **`tvbox/` 目录**下额外生成一份 `py.json`（同时把蜘蛛 `.py` 复制到 `tvbox/py/`），可直接作为 TVBox 配置使用（配合 `py.jar` 本地 Python 解析）：

- `spider`：固定为 `./py.jar`（需你自备 `py.jar` 才能启用本地 Python 解析）。
- `sites`：**自动生成**自本次采集到的所有 `.py` 蜘蛛脚本（每个 `.py` 一个 `type=3` 站点，`api` 指向 `./py/<name>.py`）。脚本同时把 `.py` 复制到 `tvbox/py/`，使 `./py/<name>.py` 可直接加载。
- `lives`（**来自采集源，自动合并**）：脚本会扫描本次采集到的所有 TVBox 配置（如老杨TV纯净版 `xxz.json`、`ok海豚665.json` 等），提取它们的 `lives` 直播源，以 `name + url` 去重后追加在模板原有直播源（灵鹿直播、家用直播）之后。**你不用手填直播源**，每次运行自动按最新采集结果刷新。
- `rules` / `doh` / `flags` / `ijk` / `ads`：来自脚本内嵌的静态模板，按需改 `fetch_tvbox.py` 里的 `PYJSON_TEMPLATE_JSON` 常量即可。

订阅方式：`https://cdn.jsdelivr.net/gh/你的用户名/tvbox-mirror@main/tvbox/py.json`

> 想**固定**某些直播源不被后续采集覆盖/增减，直接改 `PYJSON_TEMPLATE_JSON` 里的 `lives`；脚本只会把采集到的新源补在后面，已存在的不会重复。

---

## 七、怎么用订阅链接

采集完成后，脚本自动在 `docs/` 生成两份东西：

- **订阅页**：`https://你的用户名.github.io/tvbox-mirror/`（网页，可点链接/复制）
- **汇总订阅源**：`https://cdn.jsdelivr.net/gh/你的用户名/tvbox-mirror@main/tvbox/tvbox.json`
  —— 这是一个「一个链接订阅全部」的汇总，直接填进 TVBox 的订阅地址即可。
- **单个配置**：`https://cdn.jsdelivr.net/gh/你的用户名/tvbox-mirror@main/tvbox/文件名.json`

> jsDelivr 首次拉取可能有几十秒缓存延迟，属正常。

---

## 八、日常调整

- **加更多站**：
  - 单文件：往 `sources.txt` 追加一行具体地址（支持 GitHub raw 直连，或带 `gh-proxy`/`ghproxy` 前缀，脚本会自动剥离前缀直连 raw）。
  - **整仓库镜像（推荐抓核心代码）**：追加一行 `REPO:OWNER/REPO[@分支]`，脚本会自动抓取该仓库全部核心文件（`.py` `.js` `.json` `.txt` 等，自动跳过 `.jar` 等二进制）。例如：
    ```
    REPO:FGBLH/HKL
    REPO:other/dev@dev
    ```
    文件会按原目录保存到 `tvbox/<OWNER>__<REPO>/...`。若 GitHub API 报速率限制，在仓库 `Settings → Secrets → Actions` 确认 `GITHUB_TOKEN` 已可用（Actions 默认注入，额度 5000/小时）。
- **改频率**：编辑 `.github/workflows/collect.yml` 里的 `cron`。
- **查日志**：仓库 **Actions → 某次运行 → 展开步骤**，或看 `logs/collect.log`（会随仓库提交）。

---

## 九、常见问题

**Q1：定时任务不跑了？**
GitHub 会在仓库 **60 天无任何活动**后自动停用 scheduled workflows。解法：进 Actions 点一次 **Run workflow** 手动触发即可重新启用；或保持仓库有定期提交（本任务有变化时就会提交，长期无变化才会"安静"）。

**Q2：订阅页打不开 / 链接 404？**
- 确认仓库是 **Public**；
- 确认 Pages 已按第六步设为 `main` + `/docs`；
- jsDelivr 第一次可能要等 1～2 分钟缓存生效；
- 检查 `tvbox/` 里是否真的有文件（可能 discovery 没发现到，看日志）。

**Q3：某站没采到任何文件（最常见！）**
本项目的 `lytvs.top` 这类站点**自带 WAF/反爬 + 限流**，而且**很可能会封 GitHub 的数据中心 IP**。具体表现：同一地址有时 200、有时 503、有时 404、有时 TLS 握手异常——所以 GitHub Actions 跑出来偶尔是 0 文件。

排查与解法，按优先级：

1. **先看日志（Actions → 那次运行 → 展开步骤）**，关键几类：
   - `FAIL ... 503/404/SSL WRONG_VERSION_NUMBER` → 目标文件这次被 WAF 拦了（间歇性的，过会儿再跑可能就好，脚本已内置 4 次重试）。
   - `OK 文件名 (字节数)` → 成功；`SKIP 未变化` → 内容没变、跳过写入（正常）。
2. **gh-proxy 地址最稳**：脚本会自动把 `gh-proxy.com/https://raw...` 还原成 `raw.githubusercontent.com` 直连——GitHub Actions 内网直达，比代理稳得多。
3. **若 GitHub 长期 0 文件、日志全是 503/404**：基本就是 IP 被封。两条出路：
   - **换到本地 / VPS 跑**：在你自己电脑用任务计划程序（Windows）或 crontab（Linux）定时跑 `python fetch_tvbox.py`，普通家庭/机房 IP 远比 GitHub 云 IP 不容易被封；
   - **给 GitHub Actions 挂代理**：在仓库 **Settings → Secrets → Actions** 加一个名为 `HTTPS_PROXY`（值填一个不被该站封锁的代理地址，如 `http://1.2.3.4:7890`），脚本会自动读取并走代理。
4. **频率别太狠**：每 6 小时已经偏勤，若仍被限流，可在 `collect.yml` 把 cron 改成 `0 3,9,15,21 * * *`（每 6 小时但错峰），降低被封概率。

**Q4：Private 仓库能用吗？**
能跑采集（Actions 免费额度每月 2000 分钟），但 **Pages 和 jsDelivr 订阅链接需要 Public** 才能对外访问。

**Q5：加密接口（如 ok海豚18）能自动解密吗？**
能。脚本内置对「肥猫工具箱」AES-128-CBC 加密格式的自动解密：下载到十六进制密文后自动识别 key/iv、解密为 JSON，并继续抓取配置里引用的核心文件。云端工作流已自动安装 `pycryptodome`；本地运行前需 `pip install pycryptodome`。若未来加密格式变了，把新样本地址和截图给我，我再适配。

---

## 十、目录回顾

```
tvbox-collector/
├── .github/workflows/collect.yml   # 每 6 小时定时任务
├── fetch_tvbox.py                  # 采集 + 中文编码 + 重试 + 生成订阅页 + Telegram 推送 + AES 解密
├── sources.txt                     # 采集目标（每行一个地址）
├── repo.txt                        # 本地测试仓库标识
├── tvbox/                          # 采集结果（自动生成，含下面两项）
│   ├── py.json                     #   自动生成的 TVBox 蜘蛛配置（sites 来自 .py，lives 来自采集源）
│   └── py/                         #   自动复制的蜘蛛脚本（供 py.json 的 ./py/<name>.py 加载）
├── docs/                           # 订阅页 index.html + tvbox.json（自动生成）
└── logs/                           # collect.log 运行日志（自动生成）
```

部署完如果某站采不到、或想要分组订阅 / 更新推送通知，告诉我继续加。
